git config --global user.name "stephygrace14"
git config --global user.email "stephygrace@karunya.edu

git clone https://github.com/stephygrace14/sta
cd star
(creat file in star(vs))  git add helloo
git commit -m "changed"
git push origin main
git pull
git init(folder come to)
git branch origin master
git remote add origin https://github.com/st


git branch -a
git branch -M hie
git branch -a
(create new file)git add see
git commit -m "changed"
git push origin hie
